# frozen_string_literal: true

# Gemfile
#
# Bundler dependency file for the ruby-pi gem. Sources gems from RubyGems.org
# and delegates dependency resolution to the gemspec.

source "https://rubygems.org"

gemspec

# Pin versions compatible with Ruby 3.3 without native extension compilation
gem "bigdecimal", "3.1.5"
gem "json", "2.7.1"
gem "faraday-net_http", "~> 3.0"
