# frozen_string_literal: true

# Rakefile
#
# Build automation for the ruby-pi gem. The default task runs the RSpec test suite.

require "rspec/core/rake_task"

RSpec::Core::RakeTask.new(:spec)

task default: :spec
